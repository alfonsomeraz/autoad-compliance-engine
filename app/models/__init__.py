"""Shared data models.

Pydantic v2 schemas (incl. the `AdClaims` extraction contract) and SQLAlchemy
ORM models for inventory, offers, rules, runs, and audit. Pydantic everywhere;
JSONB for rule predicates / claims / evidence.
"""
