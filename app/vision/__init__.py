"""Image rendering + vision-model extraction (Phase 3).

Extracts *displayed* price/trim/disclaimer values from rendered ad images into
the same AdClaims schema, then cross-checks against source data.
"""
