"""Ad copy generation + HTML template rendering.

Copy gen with auto-validate-and-self-correct. HTML templates are populated
from source data so price/trim/disclaimer text is deterministic by construction.
"""
